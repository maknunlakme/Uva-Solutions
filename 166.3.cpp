#include<iostream>
using namespace std;

int rec(int pos,int money)
{
    if(pos==6) return 0;
    if(money==0) return 1;
    if(money<0) return 0;
    if(dp[pos][money]==-1)
    {
        for(int i=0;i<arr[])
    }
    return dp[pos][money];
}


int main()
{
    while(1)
    {
        for(i=0;i<6;i++) cin>>arr[i];
        if(arr[0]==0 and arr[1]==0 and arr[2]==0 and arr[3]==0 and arr[4]==0 and arr[5]==0) break;

    }
    return 0;
}
